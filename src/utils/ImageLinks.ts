const ImageLinks = {
  erase: require('../assets/Image/erase.svg')?.default,
  filter: require('../assets/Image/filter.svg')?.default,
  add: require('../assets/Image/add.svg')?.default,
};

export default ImageLinks;
